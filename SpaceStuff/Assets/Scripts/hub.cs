using UnityEngine;

public class hub : MonoBehaviour
{
    // Reference to your systems (e.g., Swipe, Map)
    //public static Swipe SwipeFunction;
    //public static Map mapSystem;

    void Awake()
    {
        // Initialize references to the systems
        //SwipeFunction = FindObjectOfType<SwipeFunction>();
    }
}
